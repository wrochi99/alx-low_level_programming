#include "main.h"

/**
 * _puts_recursion - prints string followed by new line
 *
 * @s - string to be printed
 *
 * Return: void
 */
void _puts_recursion(char *s);
{
	int i;
	char s[i];

	if (i == 0)
	{
		_putchar('\n');
	}

	while (i > 0)
	{
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}
